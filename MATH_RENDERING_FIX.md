# Math rendering fix

This update keeps the existing documentation and only restores/improves formula rendering.

Changed files:

- `docs/assets/javascripts/mathjax.js`
  - uses the recommended MkDocs Material + `pymdownx.arithmatex` configuration;
  - processes only the `arithmatex` class generated by MkDocs;
  - re-renders formulas after Material instant navigation.
- `mkdocs.yml`
  - keeps `pymdownx.arithmatex` enabled;
  - keeps MathJax loaded after the local configuration script;
  - uses the jsDelivr MathJax CDN.
- `docs/index.md`
  - fixes language-card links from `nl/` and `en/` to `nl/index.md` and `en/index.md`.

Important: formulas can still look raw in GitHub's normal Markdown preview. They render correctly on the built MkDocs/GitHub Pages site.
